apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imap2telegram-helm.fullname" . }}-test-connection"
  labels:
    {{- include "imap2telegram-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "imap2telegram-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
